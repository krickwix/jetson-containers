apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rec-http-helm.fullname" . }}-test-connection"
  labels:
    {{- include "rec-http-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rec-http-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
